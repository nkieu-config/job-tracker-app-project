// Shared Tailwind classes for form fields, so every form stays consistent.
export const inputClass =
  "rounded-[4px] border border-[#e6e6e6] bg-canvas px-3 py-2.5 text-[16px] font-normal text-ink outline-none focus:border-primary focus:ring-1 focus:ring-primary transition-colors";

export const labelClass =
  "flex flex-col gap-1.5 text-[16px] font-medium text-ink";
